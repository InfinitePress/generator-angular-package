language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-angular-package' ]; then cd .. && eval "mv $currentfolder generator-angular-package" && cd generator-angular-package; fi

